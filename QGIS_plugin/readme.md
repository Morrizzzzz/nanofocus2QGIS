## Install in QGIS

- Zip the folder NanoFocusCsvLoader (the folder itself must be inside the zip).
- In QGIS: Plugins → Manage and Install Plugins… → Install from ZIP
- Enable it.
- You’ll see a new menu entry: Plugins → NanoFocus CSV Loader → Load NanoFocus CSV
